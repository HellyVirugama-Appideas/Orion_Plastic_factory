<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= screen ? 'Edit' : 'Add' %> Onboarding Screen</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1><%= screen ? 'Edit' : 'Add' %> Onboarding Screen</h1>

    <form action="<%= screen ? '/admin/onboarding/update/' + screen._id : '/admin/onboarding/add' %>" 
          method="POST" enctype="multipart/form-data">

      <div class="mb-3">
        <label class="form-label">Type</label>
        <select name="type" class="form-control" required <%= screen && screen.type === 'splash' ? 'disabled' : '' %>>
          <option value="tutorial" <%= (screen && screen.type === 'tutorial') || !screen ? 'selected' : '' %>>Tutorial</option>
          <option value="splash" <%= screen && screen.type === 'splash' ? 'selected' : '' %>>Splash</option>
        </select>
        <% if (screen && screen.type === 'splash') { %>
          <small class="text-danger">Splash screen can only be one. Editing will replace the existing one.</small>
        <% } %>
      </div>

      <div class="mb-3">
        <label class="form-label">Media (Image or Video) <%= screen && screen.type === 'splash' ? '(Required)' : '' %></label>
        <input type="file" name="media" class="form-control" accept="image/*,video/*"
               <%= !screen || (screen && screen.type === 'splash') ? 'required' : '' %>>
        <% if (screen && screen.mediaUrl) { %>
          <div class="mt-2">
            <small>Current:</small><br>
            <% if (screen.mediaType === 'video') { %>
              <video controls style="max-height: 300px;">
                <source src="<%= screen.mediaUrl %>" type="video/mp4">
              </video>
            <% } else { %>
              <img src="<%= screen.mediaUrl %>" style="max-height: 300px;" class="img-thumbnail">
            <% } %>
          </div>
        <% } %>
      </div>

      <div class="mb-3">
        <label class="form-label">Title</label>
        <input type="text" name="title" class="form-control" value="<%= screen ? screen.title : '' %>">
      </div>

      <div class="mb-3">
        <label class="form-label">Description</label>
        <textarea name="description" class="form-control" rows="3"><%= screen ? screen.description : '' %></textarea>
      </div>

      <div class="mb-3">
        <label class="form-label">Button Text (Default: Next)</label>
        <input type="text" name="buttonText" class="form-control" value="<%= screen ? screen.buttonText : 'Next' %>">
      </div>

      <div class="mb-3">
        <label class="form-label">Button Color (Hex with alpha, e.g. #8c8c8cff)</label>
        <input type="text" name="buttonColor" class="form-control" value="<%= screen ? screen.buttonColor : '#8c8c8cff' %>">
      </div>

      <div class="mb-3">
        <label class="form-label">Order (for tutorial screens)</label>
        <input type="number" name="order" class="form-control" value="<%= screen ? screen.order : 0 %>">
      </div>

      <button type="submit" class="btn btn-success">
        <%= screen ? 'Update' : 'Add' %> Screen
      </button>
      <a href="/admin/onboarding" class="btn btn-secondary ms-2">Cancel</a>
    </form>
  </div>
</body>
</html>